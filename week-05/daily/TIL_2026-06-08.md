# TIL - 2026-06-08

## 오늘 배운 내용
- 핵심 개념: 언어 모델, n-gram, 형태소 분석, Word Embedding, RNN, LSTM
- 코드 실습: 다음 단어 예측 모델 구조 이해
- 새로 알게 된 용어: SLM, NNLM, One-hot Encoding, BPTT

### 1.
언어 모델은 이전 단어들을 바탕으로 다음 단어를 예측하는 모델이다.

### 2.
RNN은 시간 순서 정보를 반영하지만, 긴 문맥 처리에는 한계가 있다.

python # 다음 단어 예측 모델의 기본 흐름 input_text -> embedding -> model -> next_word 

## 헷갈린 내용
- RNN의 hidden state와 LSTM의 cell state 차이가 헷갈렸다.
- 시간이 지나도 중요한 정보를 유지하려고 LSTM이 등장했다는 식으로 이해했다.

## 정리
- 오늘은 언어 모델이 n-gram에서 신경망 기반 모델로 발전하는 흐름을 배웠다.
- 실무에서는 챗봇이나 텍스트 생성 모델의 기본 구조로 활용될 수 있다.
- 내일은 Transformer와 Attention 개념을 복습해야겠다.